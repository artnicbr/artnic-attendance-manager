import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'artnic-attendance-manager',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
